#include <stdio.h>
/**
 * main - Entry point Print the alphabet lowered.
 *Return: Always returns 0
 */
int main(void)
{
	char letter = 'a';
	while
	       	(letter <= 'z')
	{
		putchar(letter);
		letter++;
	}
	putchar('\n');
	return (0);
}
