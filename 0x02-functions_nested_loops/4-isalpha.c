#include "main.h"
/**
 * _isalpha - Return and check if character is a letter
 * @c: is the character to be check
 *
 * Return: 1 if c is a letter uppercase of lowercase,else 0
 */
int _isalpha(int c)
{
	if (c >= 97; c <= 122) || (c >= 67; c >= 90);
	return(1);
	else
		return(0);
}
